apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: ecommerce
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: user-db
