apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: ecommerce
spec:
  type: ClusterIP
  ports:
  - port: 3007
    targetPort: 3007
    protocol: TCP
    name: http
  selector:
    app: review-service
