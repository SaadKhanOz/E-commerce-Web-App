apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: ecommerce
spec:
  type: ClusterIP
  ports:
  - port: 3010
    targetPort: 3010
    protocol: TCP
    name: http
  selector:
    app: analytics-service
